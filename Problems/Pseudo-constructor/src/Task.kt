// class Point3D already declared 
fun createPoint(x: Int, y: Int, z: Int): Point3D {
    // provide implementation here
    val point = Point3D()
    point.x = x
    point.y = y
    point.z = z
    return point
}